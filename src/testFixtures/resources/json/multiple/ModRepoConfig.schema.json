{
  "$schema": "http://json-schema.org/draft-06/schema#",
  "$ref": "#/definitions/ModRepoConfig",
  "definitions": {
    "ModRepoConfig": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "$schema": {
          "type": "string",
          "title": "Schema",
          "description": "Pointer to the schema against which this document should be validated."
        },
        "name": {
          "type": "string"
        },
        "type": {
          "enum": [
            "vanilla",
            "forge",
            "spigot",
            "paper"
          ]
        }
      },
      "required": [
      ],
      "title": "ModRepoConfig"
    }
  }
}